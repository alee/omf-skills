# Architecture decision records

This directory is reserved for decisions whose context, alternatives, and consequences should remain reviewable. There are no accepted ADRs yet. A future ADR should link to the current specification it informs without duplicating that specification's rules.

Record a decision only when its rationale is supported by the repository history or an explicit maintainer decision; do not invent retrospective reasoning.
